<div class="d-flex justify-content-center m-3 form-width">
  <div class="col-6 text-white">
    <h1>Post a new offer</h1>
    <%= simple_form_for @offer do |f| %>
      <%= f.input :title%>
      <%= f.input :price%>
      <%= f.input :address,
            input_html: { style: "display: none", data: { geocoder_target: "address" } },
            wrapper_html: { data: { controller: "geocoder", geocoder_api_key_value: ENV['MAPBOX_API_KEY'] } } %>
      <%= f.input :description%>
      <%= f.input :photo, as: :file %>
      <%= link_to "Back", my_offers_path, class:"btn btn-light" %>
      <%= f.submit class:"btn btn-light" %>
    <% end %>
  </div>
</div>
