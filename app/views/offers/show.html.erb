<% if @offer.photo.attached?  %>
  <%= cl_image_tag @offer.photo.key, height: 300, width: 400, crop: :fill %>
<% end %>
<h1><%= @offer.title %></h1>
<p><%= @offer.description %></p>
<%= link_to 'Edit', edit_offer_path, class:"btn btn-primary" %>
